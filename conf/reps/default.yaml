ensemble_path: ${data_root}/structures/ensembles_tier1.jsonl

coulomb:
  permutation: sorted_l2
  standardize: false
  single_conformer: false # instead of enselmble use conformer with hghest Boltzmann weight, Sigma will be zeros
  out: ${data_root}/reps/coulomb/descriptors_tier1.npz

soap_params:
  rcut: 2.5
  nmax: 2
  lmax: 2
  sigma: 0.7

soap_options:
  standardize: false
  single_conformer: false
  compress: false
  out: ${data_root}/reps/soap/descriptors_tier1.npz

